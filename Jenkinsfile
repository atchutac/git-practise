pipeline {
    agent any

    stages {

        stage('Linux Commands') {
            steps {
                echo 'Executing Linux Commands...'

                sh 'pwd'

                sh 'ls -la'

                sh 'date'
            }
        }

    }
}

